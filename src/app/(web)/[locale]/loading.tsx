import { type NextPage } from 'next'

import { LoadingModule } from '@/app/modules/loading'

// component
const Loading: NextPage = () => {
  // return
  return <LoadingModule />
}

export default Loading
